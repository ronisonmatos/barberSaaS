// Estilos do template "Prestígio" — reaproveita os tokens de botão/rótulo do clássico
// (BOTAO_PRIMARIO etc. já usam --tenant-* e não têm nada de específico de layout largo/estreito),
// só acrescenta os contêineres largos que esse template usa em vez do cartão único de 480px.
export { BOTAO_PRIMARIO, BOTAO_SECUNDARIO, BOTAO_GHOST, ROTULO_SECAO } from "./estilos";

export const CONTAINER_LARGO = "mx-auto w-full max-w-5xl px-4 sm:px-6";
export const SECAO = "py-12 sm:py-16";
